{
  "name": "c-web-runner",
  "version": "1.0.0",
  "description": "A web interface built to run C code via a Node backend.",
  "main": "server.js",
  "scripts": {
    "build:c": "gcc main.c -o runner.exe",
    "start": "npm run build:c && node server.js",
    "dev": "npm run build:c && node server.js"
  },
  "dependencies": {
    "express": "^4.22.1"
  }
}
